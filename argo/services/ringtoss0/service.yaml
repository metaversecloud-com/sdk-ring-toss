apiVersion: v1
kind: Service
metadata:
  name: ringtoss0
  labels:
    app.kubernetes.io/name: ringtoss0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ringtoss0
  ports:
    - name: http
      port: 80
      targetPort: http
